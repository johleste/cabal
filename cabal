#!/usr/bin/env bash
# cabal <query>  — send a task straight to the Commander
# Symlink to ~/.local/bin/cabal for system-wide access.

CABAL_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec "${CABAL_PYTHON:-python3}" "$CABAL_DIR/cabal.py" run "$@"
